#!/usr/bin/python3
""" Module of function that return true if the object is exactly """


def is_same_class(obj, a_class):
    """ Return if object is exactly with class """
    return(type(obj) == a_class)
